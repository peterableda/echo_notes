# Echo Notes Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED API CONFIGURATION
# =============================================================================

# Unified API Key for both Whisper and LLM services
# This should be your CDP JWT token or API key
API_KEY=your_api_key_here

# Whisper API Configuration
# Base URL for the hosted Whisper transcription service
WHISPER_BASE_URL=https://your-whisper-endpoint.com/v1

# LLM API Configuration
# Base URL for the hosted language model service (OpenAI-compatible)
LLM_BASE_URL=https://your-llm-endpoint.com/v1

# Model ID for the language model (e.g., meta/llama-3.3-70b-instruct)
LLM_MODEL_ID=your_model_id

# =============================================================================
# OPTIONAL DIRECTORY CONFIGURATION
# =============================================================================

# Directory for storing audio recordings
# Default: ~/Documents/meetings
# MEETINGS_DIR=

# Directory for storing transcription projects
# Default: ~/Documents/transcriptions
# TRANSCRIPTIONS_DIR=


# =============================================================================
# NOTES
# =============================================================================
#
# 1. All API endpoints should be OpenAI-compatible
# 2. The same API_KEY is used for both Whisper and LLM services
# 3. Make sure your API key has access to both services
# 4. The application will fail to start if any required variables are missing
# 5. Keep your .env file secure and never commit it to version control
